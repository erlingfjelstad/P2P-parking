<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ctlVehicleContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:background="@drawable/login_gradient_background">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/imvVehicleIcon"
        android:layout_width="45dp"
        android:layout_height="18dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/ic_car" />

    <TextView
        android:id="@+id/txtVehicleName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:fontFamily="@font/lato"
        android:textColor="@color/colorLabelText"
        android:textSize="15sp"
        app:layout_constraintBottom_toBottomOf="@+id/imvVehicleIcon"
        app:layout_constraintStart_toEndOf="@+id/imvVehicleIcon"
        app:layout_constraintTop_toTopOf="@+id/imvVehicleIcon"
        tools:text="Vehicle name" />

    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        android:background="@color/colorLabelText"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/txtVehicleName" />

</androidx.constraintlayout.widget.ConstraintLayout>